# Portable Kit File Manifest

Generated by `node scripts/sync-portable-kit.mjs`. Re-run after ruleEngine changes.

## Module whitelist (§14.2)

| Module | Included | Notes |
|--------|----------|-------|
| portable/* | ✓ | Single public API |
| closure/* | ✓ | ClosureRegistry |
| bundle/* (partial) | ✓ | Excludes importAdapter, autoDesign |
| design/* | ✓ | forwardTrace, reverseRoute, bidirectional |
| utils/fixturesPath | ✓ | FIXTURES_ROOT support |
| facade, routes, storage | ✗ | Host-only |

## Typical size

- T1: ~550KB (source + fixtures)
- T3: ~700KB (+ modality fixtures)

## Environment

```bash
export FIXTURES_ROOT=/path/to/portable-kit/data/fixtures
```

## Verification

```bash
yarn test:inspect-bundle
node portable-kit/run.mjs
```

See also: [PORTABLE_QUICKSTART.md](./PORTABLE_QUICKSTART.md), [ARCHITECTURE.md](./ARCHITECTURE.md).
